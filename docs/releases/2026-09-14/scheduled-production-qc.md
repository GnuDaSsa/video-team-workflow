# Shared scheduled-production / QC upgrade

## Session audit → canonical implementation

| Observed problem | Shared change / owner |
| --- | --- |
| Native 30-minute checks succeeded, but local selection remained 20 | Seedance helper accepts explicit regular cadence and checks a fresh native snapshot against exact task, ID, ACTIVE state and cadence |
| Recurring prompt retained old pending counts and composer assumptions | Shared production uses stable state pointers; current queue/receipts/QC reports own mutable facts |
| Current-board audit compared two current jobs with all historical rows | Helper compares current scope, recognizes provider `status` aliases; conflicting aliases, duplicate IDs and unseen active claims stay errors |
| Resolved native-frame intervals were revisited while playback/audio remained undone | Runtime per-asset coverage checker reuses bound evidence, returns unresolved intervals, and separates a resolved FAIL from approval |
| More altitude/negative prose did not fix near-floor action or ambiguous scores | Prompt review requires an evidenced input/spatial cause, scoped revision and continuous depth/contact proof before adding effects |
| A usable short insert could be mistaken for approval of the parent | QC template keeps the parent HOLD; trimmed derivatives need lineage and their own verification |

Existing exact Aside ownership, enlarged reference verification, immutable
attestation/Generate transaction, scoped English override and Blender motion-guide
vs source-restyle branch were retained rather than duplicated. User-specific
cast, fictional abilities, six-shot timing and particular drone designs do not
become blanket rules for unrelated projects.

## Verification

- Harness validate/check-state performed before edits; existing unrelated blocked
  goal preserved. This scoped contract is `2026-09-14-scheduled-production-and-qc`.
- **185 unit tests PASS** (18 added), Python compilation, shell syntax,
  Seedance skill validation and `git diff --check` PASS.
- Canonical freeze/preflight/apply/check: **68 managed files match**. Six unique
  deployed files changed; the later current-scope audit correction updated the
  helper once more. No media, provider job, browser owner or new scheduler created.
- Real local reproduction: before correction, native cadence 30 vs local 20
  failed `queue-schedule-check`. Existing mode corrected through deployed helper;
  after correction the same native record matches at 30. Deployed `queue-doctor`
  now returns 1800 seconds and no state-audit issues for this project.
- Existing native automation updated successfully. Independent record read
  confirmed unchanged ID, task, name, cadence, ACTIVE status and notification
  policy; stored stable prompt matches the submitted update.
- Deployed QC checker ran against the actual registered AL19 candidate and
  existing goal report: source/probe/evidence verified, repeat interval returns
  `REUSE_EXISTING_REVIEW`, next work is playback, and creative/audio/whole-asset
  approval remains blocked. No new viewing/listening was claimed.

Deployment rollback receipts are in the local verified-release archive
(`20260914_173632_497262` and `20260914_174036_030450`). Private registration/task
identifiers, URLs and native prompt bodies remain only in project/native state.

## Release hygiene

The installed global instruction had one live-only Korean presentation-voice
pointer. It was preserved verbatim in canonical source as a separate prerequisite
commit (`a964c24`) so deployment would not roll it back. Unrelated pre-existing
dirty harness/memory/contract/extract work is excluded from the feedback commit.

## Not verified / not claimed

The helpers audit operator-declared snapshots and evidence structure/hashes;
they do not authenticate native scheduling, watch video, listen to audio, or
automatically judge creative quality. Native registration was independently read,
but the **next scheduled execution after this update has not yet been observed**.
No new video was generated and no animation quality improvement or final-media
PASS is claimed. The remaining production/QC backlog and audio-action boundary
stay with the existing authorized task and reservation.
