# Routine flow without repeated consent; truthful continuation diagnostics

## Goal and evidence
The user reports repeated video-team approval prompts and inconsistent 15–20 minute monitoring across Luna/Terra. Local evidence: an old Luna cron is paused and configured for five minutes; current Seedance queue-cycle implements a same-turn 900-second foreground wait, not a scheduler. No paired model run establishes a model-capability cause.

## Changes
1. Make same-owner continuation the explicit default in runtime next/workflow output; new-owner dispatch stays optional and requires the existing specific spawn approval.
2. Clarify that routine authorized production steps and each repeat foreground wait require no new approval. Permit a safe new-project session bootstrap in the one existing Aside tab after visible idle/preservation checks; retain ambiguity/account/transaction/safety stops.
3. Add read-only queue-doctor with canonical helper version/hash, timer/receipt/orphan state, exit gate and exact next action. It must never create a queue, wait, scheduler, model turn or browser action, and never call a timer a scheduled task.
4. Explain real app scheduling as a separate, initially approved capability with a verified active scheduler receipt and actual run evidence. Do not create, reactivate or migrate the unrelated old cron, do not add a parallel browser monitor, and do not claim a scheduled continuation is installed.
5. Replace stale wiki operating summaries with pointers rather than duplicate competing procedures.

## Acceptance
Positive and negative tests for current-owner next output, preserved dispatch approval, absent queue, contract-only fake scheduling, live/dead pending timer, elapsed debt, required wait not started, terminal state and model-invariant diagnosis. Run configured quick checks and strict release parity; preserve unrelated dirty files. Real Luna/Terra browser A/B and scheduled firing remain unverified, explicitly reported.
