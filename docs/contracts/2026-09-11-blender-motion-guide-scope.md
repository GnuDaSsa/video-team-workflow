# Blender reference: motion guide versus source editing

## Goal and evidence
The user clarified that Video1 is a path/movement reference, not an original
scene to edit. The common adapter defined both roles but supplied only a restyle
example and unconditionally required source camera/cut preservation. Correct
that authoring contradiction; do not claim it proves the model's failure cause.

## Bounded acceptance
- Single canonical Blender adapter keeps separate MOTION_GUIDE / SOURCE_RESTYLE
  examples, procedure and review criteria. Explicit movement-reference intent
  must not be turned into original-video editing or frame-exact repainting.
- New scene construction uses images for identity/style/venue and only named
  movement cues from video. Existing 15s / 6 x 2.5s user plan is retained.
- Save unsubmitted AC01/AC02 R2 drafts; preserve the accepted R1 prompt packs,
  attestations and provider evidence byte-for-byte. Do not attest over R1.
- Documentation regression tests, full existing tests and deployment parity
  pass before reporting the standing correction deployed.
- Commit/push only this correction and its release manifest. Preserve unrelated
  dirty files and the separate blocked main harness goal.

## Non-goals
No agent, browser mutation, scheduling, cancellation, paid retry or output-QC
approval. No automated semantic/visual fidelity validator or deterministic
motion transfer guarantee. Draft validation is not provider submission.

## Verified result
159 tests PASS (including two new scope/example documentation regressions),
Python compile, shell syntax and diff checks PASS. Both R2 draft packages pass
format validation; seven accepted R1 files retain their recorded SHA256 hashes.
Canonical release applied two changed files and verifies all 67 managed files.
No provider submission or new video/full-playback QC ran. No claim that the
new instructions achieve deterministic movement or fix model appearance.
