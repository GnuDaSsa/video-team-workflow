# Lean video workflow / Aside release contract

## Goal
Apply the 2026-09-06 audit: remove obsolete active delegation surfaces, reconcile
source/live runtime, enforce scoped Seedance prompting, and make the canonical
Aside helper actually use deterministic CLI binding to one exact existing tab.

## Scope
- Preserve media, submitted jobs, user-approved locks and other projects.
- No new agents, scheduler, browser agent, provider generation or public action.
- Archive before removing active legacy files; do not erase historical evidence.
- Adopt only the reviewed v4 runtime dependency closure needed for this release;
  preserve unrelated pre-existing working-tree changes outside the release.
- Replace duplicate role/operation prose with pointers to the canonical owners.
- Add deterministic prompt checks for unresolved camera choices and scoped entity
  contamination; retain legitimate compound direction and negative constraints.
- Add exact Aside tab/session binding with CLI-only primary JavaScript execution,
  no bare `aside`/`aside exec` agent invocation, no front-tab fallback, no hidden
  file input or clipboard image bypass. Keep native chooser handling separate.
- Add strict release parity and read-only shelf audit. Existing prepared packs
  that fail the new validator require reauthoring, not automatic re-attestation.

## Acceptance
1. Unit suite and new bad/good fixtures pass, including actual bridge routing.
2. Rendered lane prompts exclude obsolete persona/bridge/project instructions.
3. Exact session mismatch/ambiguous tab/CLI error fails closed before DOM action.
4. Release manifest checks installed file hashes and retired active paths.
5. Shadow validation reports current incompatible packs without modifying them.
6. Reviewed commit is self-contained, tested and synced to the configured repo.
7. No production E2E or media-quality improvement is claimed without playback.

## Evidence
Local audit: `/Users/gnudas/Documents/영상작업/영상팀_정밀감사_2026-09-06/`.
Store release evidence under `docs/releases/2026-09-06/` without private project
prompts, account details or personal information.
