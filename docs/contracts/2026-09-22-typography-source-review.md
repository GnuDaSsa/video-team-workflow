# Revalidate typography source provenance

## Goal
Unblock the active native Aside typography production request after a live upstream typography wiki update, without bypassing knowledge validation.

## Scope
Review the live operating manual against repository source and its selected mode/role sections, then refresh only catalog revision/review metadata and that source hash. Preserve compiled card wording, validation code, old receipts and all user media. Deploy only to the active Aside account u1.

## Source review
- Current SHA: ca6f8dc840903fa5e3b0b5f50225ab76d2838aab32e9944e53aa9be1b542d484
- Previously recorded SHA: a65537196d4a30ac90ffd8cdda41f08199890a54852f6284771450e5a048f7c0
- Read the current full manual and source diff. Changes include historical-video application, visual-edge alignment correction, retired Jeongseon-specific guidance removal, an expanded local path and updated metadata.
- The selected `모드별 선택 로직` and `타이포 역할 시스템` sections still support the existing three-bullet video-text card; no new wording or operating procedure is imported.
- Caption styling/editable postproduction remains conditional and cannot override the project's explicit native Seedance typography requirement.

## Acceptance
Native knowledge/harness/submission/deployer regressions, live typography request creation, source/install parity for u1; preserve real Astra re-acknowledgement. No fake evidence, changed safety checks, skipped text tag, snapshot fallback or old receipt rewrite.

## Verification
451 native/deployer tests passed with canonical macOS temporary directory; active u1 source/install parity passed. The first test attempt used the /tmp symlink and fixture path guards correctly rejected it; no guard was relaxed. Existing legacy Python/deployment suites were not run. Media QC and submission are distinct production stages.
