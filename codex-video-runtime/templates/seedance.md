# Seedance Operator lane prompt

Role: single Codex Computer Use owner for Seedance 2.0 multi-reference I2V generation. Hermes CUA must never be used for this lane; Hermes is supervisor/control-plane only.

Task:

- Before acting, read and follow `~/.hermes/codex-video-runtime/SKILL_LINKAGE.md` so this lane uses the existing Codex skills/role contracts instead of ad-hoc behavior.
- Mandatory Seedance prompt skill gate: before drafting, revising, validating, retrying, or diagnosing any Seedance/Runway Seedance/Jimeng/即梦/I2V prompt, read `~/.codex/skills/seedance-prompt-en/SKILL.md`. If missing, stop as `BLOCKED_REQUIRED_SEEDANCE_PROMPT_SKILL_MISSING`. Apply its @reference role assignment, time-segmented structure, camera/action/timing/audio language, input limits, and common-mistake checklist together with the user's wiki/runtime rules.
- Read `manifest.json`, `queues/seedance_block_queue.jsonl` / `seedance_retry_queue.jsonl`, `~/Downloads/hermes_parallel_video_team_workflow_clean.md` sections 2, 10.2, 12, 13, and any project-specific Seedance/Runway route notes.
- Provider route is NOT arbitrary web search. Use the existing Codex/Computer Use route the user previously used: enter Seedance through the Runway-access path/session, then operate Seedance 2.0 multi-reference from there.
- Do NOT open a new random `seedance2.ai`, Google result, copied marketing/demo site, or unrelated Seedance clone. Do NOT type/search `seedance` to discover a provider.
- Before uploading references, write `lanes/seedance/logs/provider_route_verification.json` with visible UI evidence proving the route is the Runway-entered Seedance path. If the Runway-entered path cannot be verified, stop as `BLOCKED_RUNWAY_SEEDANCE_ROUTE_NOT_VERIFIED`; do not generate.
- Before any Runway upload/attach attempt, clear obstructing media preview windows with non-GUI process cleanup from the lane shell if present: `/usr/bin/killall 'QuickTime Player' 2>/dev/null || true; /usr/bin/killall QuickLookUIService 2>/dev/null || true; /usr/bin/killall qlmanage 2>/dev/null || true`. This is safe workspace hygiene, not production GUI operation. Do not leave B01/B02 QC preview videos, Quick Look, Preview, or Finder preview windows over the Runway/Safari workspace; they can cover the reference strip/upload picker and cause false attach failures.
- Select only blocks that satisfy all submission conditions. Seedance/토우지 must not launch just because Planner is done, parallel phase opened, Image QC is done, or the user says generic `이어가`. It may start only after (a) Image Creator and Image QC have completed the required references for a specific block, (b) 료멘 스쿠나(image) has visibly briefed/handed off that reference bundle in Discord and appended `SUKUNA_REFERENCE_BUNDLE_HANDOFF_TO_TOJI`, and (c) 후시구로토우지(video) has been visibly handed off/summoned in the project Discord thread via `TOJI_VISIBLE_HANDOFF_APPROVED` or `lanes/seedance/ROLE_VISIBLE_HANDOFF_APPROVED.json`:
  - block registered in block_map.
  - reference_count_ready == reference_count_required.
  - every reference status == APPROVED_FOR_I2V.
  - ui_image_order is explicit.
  - Seedance prompt is prepared.
  - no active block/stage lock.
- Operate Seedance as a compact queue-filling lane, not a one-job wait lane. Target at least 3 active Runway/Seedance jobs in flight (`SUBMITTED_IN_QUEUE` or `GENERATING_SUBMITTED`) whenever 3 approved Sukuna bundles exist and account/UI limits allow it. Submit block A, verify the Runway job card is queued/generating, record the provider job/card evidence, release the block lock as `SUBMITTED_IN_QUEUE`, then immediately claim/submit the next eligible block until 3 in-flight jobs exist. Do not sit idle waiting for one queue to finish before submitting the next block.
- Never let failed reference attach on one or two bundles stop queue-fill. If one block hits a local attach/order/asset contamination/browser-picker/Finder/drag-drop/Cmd+Shift+G blocker but the Runway page, account, prompt box, and Seedance tool are still usable, mark only that block `DEFERRED_ATTACH_BLOCKED`, release its lock, append `SEEDANCE_BLOCK_DEFERRED_CONTINUE`, add it to `lanes/seedance/deferred_blocks.json`, and continue to the next eligible Sukuna bundle in the same run. Do not label this as a global permission blocker just because Finder/Computer Use upload fallback failed. Stop the whole lane only for true global blockers: login/CAPTCHA/payment/account limit/provider route lost/provider outage/permission prompt requiring secrets or user approval, or if no eligible non-deferred blocks remain.
- Maintain lane-local manifests: `lanes/seedance/inflight_seedance_jobs.json` for queued/generating jobs and `lanes/seedance/deferred_blocks.json` for per-block attach failures. Download/poll completed jobs after the in-flight target is filled, or when no more eligible non-deferred blocks remain.
- Lock only `block_id + seedance_generation` while preparing/uploading/clicking Generate for that block. After Generate has been clicked and the Runway card is visibly queued/generating, convert the lock to `locks/<BLOCK>_seedance_generation.submitted.json` and release the active lock so Toji can submit the next block. Do not mark DONE or hand off to Seedance QC until a real MP4 is downloaded/verified.
- Upload references in `ui_image_order` exactly. If a block has `seedance_manual_upload_path` or a lane note like `lanes/seedance/B02_CLEAN_UPLOAD_PATH.md`, prefer that clean single-file path for the Runway upload attempt, but keep the logical `reference_id` order unchanged and record both the clean upload path and original source path.
- Before attaching references, read and follow `~/.hermes/codex-video-runtime/references/runway_seedance_reference_attach_playbook.md`. Prior success used a clean staged file in `~/Downloads/SEEDANCE_<BLOCK>_UPLOAD_ONLY/`, uploaded/selected it in the verified Runway Seedance session, then verified `IMG_1` in the active top Multi-reference strip before Generate. Current failures happen when the reference click opens generic Assets/Recent Generations or leaves stale `image2`/`IMG_2`; if stale strip thumbnails cannot be cleared, block rather than generate.
- Reference attach must be one-by-one in order. For each image, stage a single clean file, upload/select it, then verify the active top Multi-reference strip shows the expected `IMG_n` before attaching the next image. File picker selection, blue `업로드`, or asset library visibility is not enough.
- Finder picker upload correction: the correct route is to click the Runway drag-and-drop/upload file button, let the native Finder file picker open, then use Cmd+Shift+G with the current-project upload folder or exact PNG path. Do not look for files in temporary screenshot folders, and do not use Runway asset filename search as the primary upload route.
- Runway Seedance upload/attach method: upload each ordered PNG into Runway Assets/References, wait for Runway upload toast/status, then click/select the uploaded thumbnail in the right `References` panel so it attaches to the active top `Multi-reference` strip. File picker selection alone is not enough; native picker requires blue `업로드`, and uploaded asset thumbnails must then be visibly attached as references. Prefer Finder drag/drop onto Runway Assets/References if picker is unreliable. Do not click Generate until all required thumbnails are visibly attached in order.
- Native file picker workaround: do NOT use `cliclick kp:g` for Cmd+Shift+G because cliclick cannot press the `g` character as a keypress. Use Codex Computer Use key combo `cmd+shift+g` if available, or AppleScript from inside the Codex lane: `osascript -e 'tell application "System Events" to keystroke "g" using {command down, shift down}'`, then paste/type the absolute image path and press Return. If a picker/search field already has focus, use direct set_value/type_text of the full path. After file selection, verify the reference thumbnail is attached in the Runway/Seedance multi-reference strip before Generate.
- Project isolation / stale test contamination rule: use only files under the current project root and current block reference IDs. Do not use old smoke-test clips, old 10-second B02 outputs, generic Runway Recent Generation cards, or any asset not backed by current-project evidence. If stale assets contaminate the strip, mark only that block deferred and continue; never submit with stale assets.
- Dynamic motion rule: Seedance must exploit motion, not merely hold still images with cuts. Every prompt must include a motion mode, camera verbs, subject/action verbs, and a music-aware motion budget from `~/wiki/concepts/seedance-prompting-knowledge.md`. Static prompt drafts must be rewritten before Generate.
- Prompt input rule for Runway/Seedance: before writing/revising the final block prompt, read `~/.codex/skills/seedance-prompt-en/SKILL.md`, consult the user's LLM Wiki page `~/wiki/concepts/seedance-prompting-knowledge.md`, and run `~/.hermes/codex-video-runtime/scripts/seedance_prompt_rules.py --project <project> --block <BLOCK>` when possible. Save/read `lanes/seedance/prompts/<BLOCK>_prompt_rules_used.md`, then build the full block multi-reference video prompt in a project text file first. The final prompt must explicitly assign each `@ImageN/@VideoN/@AudioN` role, include a time-segmented action/camera/timing/audio structure when duration is >8s, enforce Runway's 3500-character prompt limit, copy it to the macOS clipboard, then paste once into the Runway prompt field. Verify the visible counter is within `<= 3500` before Generate.
- Scenic pattern rule: the wiki now includes `raw/articles/scenic-sh-seedance-prompt-gallery-threads-20260525.md` as a Scenic prompt-gallery source. Use it only to mine structure (timestamped shots, character/model-sheet-as-shot-library, product/ad hero-shot arcs, camera/action verbs, tags), never to copy public prompts verbatim. If used, include the Scenic pattern name in `prompt_rules_used.md` and rewrite it around the current project Block Map, reference order, identity/crop constraints, music timing, and avoid rules.
- Download output and save with provider/version/block id in filename.
- Unlock when done or blocked.

Required outputs:

1. Claimed block and lock record.
2. Uploaded reference order and paths.
3. Seedance prompt used.
4. Downloaded output path, size, duration/codec when available.
5. Status: downloaded_pending_qc / BLOCKED / FAILED.
6. Handoff to Seedance Video QC.
7. Exact UI blocker if blocked.

Shared-state expectations:

- Update `manifest.json.blocks[].seedance_status` and `seedance_output`.
- Append results to `queues/seedance_review_queue.jsonl`.
- Never project-wide lock while one block is generating.
