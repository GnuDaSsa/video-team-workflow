# Deprecated Visual template

The `visual` alias/lane is not used in the sequential Codex video-team runtime. Use the explicit serial lanes instead: `image_creator_01`, `image_creator_02`, `image_qc`, `seedance`, and `seedance_qc`.

Do not create a Visual role or concurrent worker from this template.
